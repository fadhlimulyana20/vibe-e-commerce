[[redirects]]
  from = "/api/*"
  to = "https://vibe-e-commerce-production.up.railway.app/:splat" # Your production backend API URL
  status = 200
  force = true # Optional: ensures the redirect happens even if a file exists at the path

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
